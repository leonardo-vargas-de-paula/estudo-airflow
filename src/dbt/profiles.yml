#profiles.yml
default:
  outputs:
    dev:
      account: "{{ env_var('SNOWFLAKE_ACCOUNT') }}" 
      database: DBT_DEV_DB
      password: "{{ env_var('PASSWORD') }}"
      role: DBT_ROLE
      schema: JM
      threads: 1
      type: snowflake
      user: DBT_USER
      warehouse: DBT_WH
  target: dev
  
